apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devops-status-app-prod
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/MohnadBabgi/Jenkins-project.git
    targetRevision: main
    path: app/k8s/overlays/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    # Git is the source of truth. A promotion commit to overlays/prod/ deploys
    # on its own, and any manual change in the cluster (kubectl scale, edit,
    # delete) is reverted to match Git within seconds.
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
