{
  "name": "devops-status-app",
  "version": "0.4.0",
  "description": "Tiny status page app for learning a Docker/Jenkins/Kubernetes pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --target=node20 --outfile=dist/server.js",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "supertest": "^7.0.0"
  }
}
